<section class="main-section">
  <% flash.each do |name, message| %>
  <div class="alert alert-<%= name %> alert-box" data-alert>
    <%= message %>
    <a href="#" class="close">&times;</a>
  </div>
  <% end %>

  <%= yield %>

</section>